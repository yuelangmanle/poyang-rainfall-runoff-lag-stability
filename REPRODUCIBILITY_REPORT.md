# 复现验证报告

验证日期：2026-07-30。

## 端到端烟雾复现

- 在全新临时目录运行`reproduce.sh smoke`。
- 从Mendeley Data Version 1重新下载14个核心Excel文件；14/14文件的大小和SHA-256均与服务器记录一致。
- 数据审计产生38,353条流域—日记录；14/14文件日期完整，缺测0，负值0。
- 低重复次数模式完成36个流域—场景Bootstrap任务、两类最大统计量零假设和5幅基础分析图。
- 烟雾验证器通过。烟雾模式只检查管线，不作为论文数值依据。

## 正式冻结结果核验

- 标准化数据SHA-256：`e480776705587755f4d696afd5d73a25dc0bb070aaf27edb0f6ecf93e1e43237`。
- 正式参数：Bootstrap 2,000次；两类最大统计量零假设各2,000次；水文汇总Bootstrap 5,000次；主种子`20260728`。
- 正式验证器重新计算七流域点估计并核对概率与表结构，结果通过。
- 23/23个冻结核心结果文件的大小和SHA-256与正式复现清单一致。
- 最终四图由冻结正式表在独立临时目录重绘：PNG 4/4逐字节一致，SVG 4/4逐字节一致，PDF在300 dpi渲染后像素4/4一致。PDF容器可能因生成时间元数据产生字节差异，因此以渲染内容核对。

## 结论边界

上述核验支持本仓库在指定环境、代码版本、公开数据版本和随机种子下复现论文统计产物。它不证明上游元数据完整，也不把相关峰位解释为因果响应时间或物理汇流时间。
